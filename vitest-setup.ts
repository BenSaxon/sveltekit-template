import 'vitest-dom/extend-expect';

// todo: this should work globally. some configuration is missing/incorrect. for the time being just import the above import into test files where you want to extend the matchers
